<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Avisame cuando llegues porf<strong>app</strong></title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />

    <script>
      function initMap() {
        const input = document.getElementById("location");

        const options = {
          componentRestrictions: { country: "es" },
          fields: ["address_components", "geometry", "icon", "name"],
          strictBounds: false,
        };

        const autocomplete = new google.maps.places.SearchBox(input, options);

        autocomplete.addListener("places_changed", () => {
          const places = autocomplete.getPlaces();
          const { lat, lng } = places[0].geometry.location.toJSON();

          console.log(lat, lng);

          document.getElementById("latitude").value = lat;
          document.getElementById("longitude").value = lng;
        });
      }
    </script>

    <script
      async
      src="https://maps.googleapis.com/maps/api/js?key=AIzaSyBgIx2DdZVI15cmuu8uqKttln9mdamnIlY&loading=async&libraries=places&callback=initMap"
    ></script>
  </head>
  <body>
    <nav class="navbar bg-body-tertiary mb-4">
      <div class="container-fluid">
        <span class="navbar-brand mb-0 h1"
          >Avísame cuando llegues porf<strong>App</strong></span
        >
      </div>
    </nav>

    <div class="container-fluid">
      <alert class="alert alert-danger" role="alert">
        Ops! Algo salió mal. Por favor, <a href="/">intenta de nuevo</a>.
      </alert>
  </body>
</html>
